# Deprecated location — secrets belong in backend/.env
#
#   cd backend
#   cp .env.example .env
#
# Frontend public config: frontend/.env.local (copy from frontend/.env.local.example)
# See docs/security-secrets.md
